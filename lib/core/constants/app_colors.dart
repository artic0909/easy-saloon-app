import 'package:flutter/material.dart';

class AppColors {
  // Primary Gold/Bronze
  static const Color primary = Color(0xFFC5A35C); 
  static const Color primaryLight = Color(0xFFE5D1A2);
  static const Color primaryDark = Color(0xFF947A46);
  
  // Neutral / Background
  static const Color background = Color(0xFF0A0A0A); // Deep black for premium feel
  static const Color surface = Color(0xFF1E1E1E); // Dark surface
  static const Color cream = Color(0xFFFDF5E6); // Cream for light backgrounds
  
  static const Color error = Color(0xFFE57373);
  
  // Text Colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFB0B0B0);
  static const Color textOnPrimary = Color(0xFF0A0A0A);
}
